<% content_for :title do 'Редактирование блока' end %>

<%= render 'form', block: @block %>

<%= link_to fa_icon('trash-o'), project_page_block_path(@project, @page, @block), method: :delete, data: { confirm: 'Вы уверены?' } %>
