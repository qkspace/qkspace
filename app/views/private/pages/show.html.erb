<%= link_to public_project_page_url(@project, @page), title: t('.public_hint'), class: 'small text-muted' do %>
  <%= fa_icon('desktop') %> <%= public_project_page_url(@project, @page) %>
<% end %>

<div class="mt-5">
  <%= @page.html.html_safe %>
</div>

<%= button_to t('views.pages.edit_page'), edit_private_project_page_path(@project, @page), method: :get, class: 'btn btn-light mt-5' %>

<% content_for :sidebar do %>
  <%= render partial: 'sidebar' %>
<% end %>
