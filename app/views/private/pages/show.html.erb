<%= link_to public_project_page_url(@project, @page), title: t('.public_hint'), class: 'desc' do %>
  <%= fa_icon('desktop') %> <%= public_project_page_url(@project, @page) %>
<% end %>

<div class="page-content buffer-top">
  <%= @page.html.html_safe %>
</div>

<%= link_to t('views.pages.edit_page'), edit_private_project_page_path(@project, @page),
  class: 'button outline' %>

<% content_for :sidebar do %>
  <%= render partial: 'sidebar' %>
<% end %>
