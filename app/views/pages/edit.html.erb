<% content_for :title do 'Редактирование страницы' end %>

<%= render 'form', page: @page %>

<%= link_to fa_icon('trash-o'), project_page_path(@project, @page), method: :delete, data: { confirm: 'Вы уверены?' } %>
