<% content_for :title do %>
  <%= @project.title %>
<% end %>

<p><%= link_to fa_icon('pencil', text: 'Редактировать проект'), edit_project_path(@project), class: 'small' %></p>

<% if @project.pages.any? %>
  <% @project.pages.each do |page| %>
    <p><%= link_to page.title, page_path(page) %></p>
  <% end %>
  <%= link_to 'Новая страница', new_page_path, class: 'button outline' %>
<% else %>
  В этом проект нет содержимого
  <%= link_to "cоздайте страницу", new_page_path %>
  для начала.
<% end %>
